# NOTE:
# Do not force CPU-only PyTorch wheels here.
# In GPU cloud environments, install a CUDA-enabled torch build first,
# then install the rest of this file.

# ─── Core (required for C++ engine + benchmarks) ──────────────────────────────
cmake>=3.26.0
pybind11>=2.10.0          # C++/Python bridge — MUST be installed before CMake
networkx>=2.8             # Graph utilities: used in dataset_gen.py + verification
pandas>=1.5.0             # CSV output processing
matplotlib>=3.5.0         # Plotting benchmark results
numpy>=1.24.0,<2.0.0            # Numerical arrays for data processing and ML

# ─── GNN Training Pipeline and optional ML dependencies ───────────────────────
torch==2.2.0
torchvision==0.17.0
torchaudio==2.2.0
torch-geometric>=2.0.0

# Optional PyG C++ extensions (install separately for best performance):
#   pyg-lib, torch-scatter, torch-sparse, torch-cluster, torch-spline-conv
# These require torch/CUDA-matched wheels from https://data.pyg.org.

ogb>=1.3.6               # Open Graph Benchmark dataset downloader

# ─── Kaggle data download support ──────────────────────────────────────────
kaggle>=1.5.16           # Kaggle API client for dataset download and competition access

# ─── Development / Testing ───────────────────────────────────────────────────
pytest>=7.0.0             # Run unit tests